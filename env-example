#---------------------------
# DNS over HTTPS environment
#---------------------------

# Your apex/root domain — NOT the full subdomain.
# The compose file builds the FQDN as: resolver.<DOMAIN>
# Example: DOMAIN=example.com  →  resolver.example.com
# You must use resolver.DOMAIN/DH_HTTP_PREFIX in your app
DOMAIN=example.com

# HTTP path prefix that the DoH server listens on.
# Leave as "/dns-query" unless you have a reason to change it.
DOH_HTTP_PREFIX=/dns-query

# Port the DoH container listens on *inside* its network namespace.
# 8053 is the conventional choice (53 + 8000 offset).
DOH_SERVER_LISTEN=8053

# Credentials
# 1. Generate hash:  htpasswd -Bbn myuser mypassword
# 2. IMPORTANT: Double every $ sign in the hash before pasting here.
#    Docker Compose re-interprets $ in substituted values.
#    Example: $2y$05$abc  →  $$2y$$05$$abc
DOH_USER=myusername
DOH_HASHED_PASS=$$2y$$05$$REPLACE_THIS_WITH_YOUR_ESCAPED_HASH
